server:
  port: 8091

seata:
  config:
    type: nacos
    nacos:
      server-addr: nacos:8848
      namespace: beike-prod
      group: SEATA_GROUP
      data-id: seata.properties
  registry:
    type: nacos
    nacos:
      server-addr: nacos:8848
      namespace: beike-prod
      group: SEATA_GROUP
      application: seata-server
  store:
    mode: db
    db:
      datasource: druid
      db-type: mysql
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://mysql:3306/seata?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      user: root
      password: root123
  security:
    secretKey: beike-seata-key-2024
  server:
    service-port: 8091
