# 贝壳 v1.0 任务启动指令模板

> 新开 CodeBuddy 窗口 → 粘贴对应任务指令 → 开始开发

---

## 任务 0：后端启动验收

```
【项目背景】贝壳线索-项目一体化管理系统 v1.0，后端 39 个接口已交付。

【当前任务】验证后端 39 个接口全部正常。
1. 按 docs/启动验收指南.md 的四轮验收步骤，逐项跑 curl 验证
2. 发现 bug 立即修复，修复后复测
3. 确认核心链路（线索录入→跟进→状态流转→转项目→项目查询）全通

【文件范围】backend-monolith/ 下所有文件
【参考文档】docs/启动验收指南.md、docs/01-统一数据模型.md、docs/02-接口契约文档.md
【验收标准】39 个接口全通，核心链路 5 步无断点，权限/事务/状态流转正确

请先确认是否已启动 MySQL 并导入 init.sql，然后开始逐项验证。
```

---

## 任务 1：前端认证链路打通

```
【项目背景】贝壳线索-项目一体化管理系统 v1.0，后端 39 接口全通。前端 API 层已封装（src/api/auth.ts、src/api/request.ts），useAuth Hook 已对接真实 API（含降级兼容）。

【当前任务】改造登录/注册页面，完全对接真实后端 API，替代本地模拟登录。

【改动文件】
- src/pages/Login/index.tsx（主改）
- src/pages/Login/RegisterForm.tsx（主改）
- src/App.tsx（路由守卫，可能微调）

【不改】通用组件、其他页面、后端代码

【强制约束】
1. 移除页面中硬编码的 admin/admin123 演示账号逻辑，统一走 useAuth().login()
2. 登录失败显示后端返回的错误信息（如"用户名或密码错误"）
3. 保留 useAuth 中的降级兼容代码（后端不可用时自动走本地模拟）
4. 不改 src/api/ 层代码、不改 useAuth.ts

【验收标准】
1. 真实账号 admin/123456 可登录成功，Token 存入 localStorage
2. 错误账号登录显示后端错误提示
3. 刷新页面保持登录状态
4. 无 Token 访问业务页面自动跳转 /login
5. pnpm build 零 TS 错误

请先输出改动文件清单，确认后再执行。
```

---

## 任务 2：线索列表页对接

```
【项目背景】贝壳 v1.0，后端线索 API 全通，前端 API 层已封装 fetchClueList/createClue/updateClue/deleteClueBatch。

【当前任务】将线索列表页从 localStorage 模拟数据切换为真实 API 调用。

【改动文件】仅 src/pages/LTC/Leads/List/index.tsx

【接口参考】docs/02-接口契约文档.md §三 线索模块

【强制约束】
1. 移除 useLeadStore 中的业务数据读写（load/save 等），列表数据改为 useState + fetchClueList
2. 分页 pageNum/pageSize 对接接口参数
3. 搜索 keyword/承接人 owner/状态 status/等级 level/日期范围 全部对接接口
4. 新增→跳转 /ltc/leads/new，编辑→跳转详情，删除→调接口
5. 不改其他页面、不改通用组件

【验收标准】
1. 列表数据从 /api/clue/page 加载
2. 分页切换、每页条数切换正常
3. 搜索/筛选/日期范围过滤正常
4. 新增线索后列表自动刷新
5. 批量删除后列表自动刷新
6. pnpm build 零 TS 错误

请先输出改动方案，确认后再执行。
```

---

## 任务 3：线索详情页 + 跟进时间轴对接

```
【项目背景】贝壳 v1.0，后端线索详情/跟进/转项目/附件 API 全通。前端 API 层已封装 fetchClueDetail/createFollow/fetchFollowList/convertClueToProject/uploadFile。

【当前任务】将线索详情页 5 个标签页全部从 localStorage 切换为真实 API。

【改动文件】
- src/pages/LTC/Leads/Detail/index.tsx
- src/pages/LTC/Leads/Detail/FollowUpTab.tsx
- src/pages/LTC/Leads/Detail/TriangleTab.tsx
- src/pages/LTC/Leads/Detail/FilesTab.tsx
- src/pages/LTC/Leads/Detail/LogsTab.tsx
- src/pages/LTC/Leads/Detail/LeadCreateModal.tsx（新增线索弹窗）

【接口参考】docs/02-接口契约文档.md §三 线索模块 + §五 附件模块

【强制约束】
1. 基础信息编辑：调 updateClue，表单值与 lead 实体字段一一对应
2. 跟进记录：列表调 fetchFollowList，新增调 createFollow（含 newStatus 状态变更），直接移除 localStorage 读写
3. 铁三角：改为调 interface 或暂时保留 localStorage（标注 TODO）
4. 附件：上传调 uploadFile，列表调 fetchAttachmentList，删除调 deleteAttachment
5. 转项目：点击弹出确认弹窗，调 convertClueToProject，成功后刷新显示
6. 操作日志：暂时保留 localStorage 或对接 /api/log/page

【验收标准】
1. 详情页加载显示正确数据
2. 新增跟进 + 选状态变更 → 线索状态同步更新 + 自动生成状态变更记录
3. 跟进时间轴倒序展示，新增后自动刷新
4. 转项目成功后线索状态变为已转项目
5. 附件上传/列表/删除正常
6. pnpm build 零 TS 错误

请先输出改动文件清单和每个文件的改动要点，确认后再执行。
```

---

## 任务 4：项目模块对接

```
【项目背景】贝壳 v1.0，后端项目 API 全通，前端 API 层已封装 fetchProjectList/fetchProjectDetail/createProject/updateProject/deleteProject。

【当前任务】将项目列表和详情页从 localStorage 切换为真实 API。

【改动文件】
- src/pages/PM/Projects/index.tsx
- src/pages/PM/Projects/Form.tsx
- src/pages/PM/Kanban/index.tsx
- src/pages/PM/Risks/index.tsx
- src/pages/PM/Talent/index.tsx

【不改】通用组件、后端代码、其他业务页面

【强制约束】
1. 移除 useDataStore 中项目相关的业务数据读写
2. 列表/详情数据从接口获取
3. 新增/编辑调接口，成功后刷新列表
4. 删除为逻辑删除，调接口
5. 风险/人才模块暂时保留 localStorage（标注 TODO）

【验收标准】
1. 项目列表分页/搜索/筛选正常
2. 项目详情正常显示
3. 新增/编辑/删除功能正常
4. 从线索转来的项目 sourceClueId 正确
5. pnpm build 零 TS 错误

请先输出改动文件清单，确认后再执行。
```

---

## 任务 5：工作台首页对接

```
【项目背景】贝壳 v1.0，后端线索统计 API /api/clue/stats 可用，项目管理数据走 /api/project/page。

【当前任务】首页线索管理卡片从 localStorage 切换为 API，统计卡片对接真实数据。

【改动文件】仅 src/pages/Portal/index.tsx

【不改】快捷入口卡片（已是弹窗模式，无需跳转）、后端代码

【强制约束】
1. 线索总数/待跟进/最近更新从 /api/clue/page 或 /api/clue/stats 获取
2. 统计卡片（重点项目/人才资源/预警）暂时保留 useDataStore（标注 TODO 后续对接）
3. 新建线索按钮仍弹 LeadCreateModal，创建后调用 refreshLeads() 刷新卡片
4. 不改快捷入口下方的线索/项目快速录入卡片

【验收标准】
1. 线索管理卡片数据从接口实时加载
2. 创建线索后卡片数字自动刷新
3. 查看全部→跳转 /ltc/leads 正常
4. pnpm build 零 TS 错误

请先输出改动方案，确认后再执行。
```

---

## 任务 6：系统管理前端页面

```
【项目背景】贝壳 v1.0，后端系统管理 13 个接口全通。前端 API 层已封装 fetchUserList/createUser/updateUser/fetchRoleList/assignPermissions/fetchRecycleList/permDeleteItems/fetchLogList 等。

【当前任务】新建 4 个系统管理页面：用户管理、角色权限、回收站、操作日志。

【新建文件】
- src/pages/Admin/SystemUsers.tsx
- src/pages/Admin/SystemRoles.tsx
- src/pages/Admin/SystemRecycle.tsx
- src/pages/Admin/SystemLogs.tsx

【可能改动】App.tsx（添加路由）、BasicLayout.tsx（菜单入口，仅管理员可见）

【强制约束】
1. 复用现有通用组件和 Ant Design 样式规范
2. 用户表单：用户名、姓名、密码、邮箱、手机号、角色选择
3. 角色权限：Checkbox 多选权限，调 assignPermissions 接口
4. 回收站：Tab 切换线索/项目，批量恢复/永久删除
5. 操作日志：Table 展示，支持按时间/操作人/操作类型筛选
6. 所有页面仅管理员可访问（菜单显隐 + 路由守卫）

【验收标准】
1. 用户增删改查/启停/重置密码功能正常
2. 角色权限分配正常，修改后对应用户权限实时生效
3. 回收站查看/批量恢复/永久删除正常
4. 操作日志查询/筛选正常
5. pnpm build 零 TS 错误

请先输出页面结构设计，确认后再执行。
```

---

## 任务 7：全链路联调 + 体验打磨

```
【项目背景】贝壳 v1.0，以上 6 个任务全部完成，业务接口全量对接。

【当前任务】端到端全链路走通 + 边界场景兜底。

【改动范围】全项目，但以微小修复为主

【事项清单】
1. 全链路走通：登录→首页→新建线索→详情→新增跟进(改状态)→转项目→项目详情
2. 空状态兜底：列表无数据/详情无数据 显示 Empty 组件+操作引导
3. 加载态：所有列表/详情接口调用时显示 Spin/Skeleton
4. 错误态：接口报错不白屏，显示 message.error + 重试按钮
5. 二次确认：删除/批量操作/转项目 有 Popconfirm
6. 权限联动：不同角色登录，菜单/按钮显隐正确
7. 降级开关：后端不可用时自动走 localStorage 兜底

【验收标准】
1. 全链路从头走到尾无断点
2. 所有页面无控制台报错
3. pnpm build 零 TS 错误
4. admin/zhangming/employee 三个角色权限正确

请先输出需要修复的问题清单，确认后逐项修复。
```
